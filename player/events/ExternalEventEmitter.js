
import Observable from "../utils/Observable";

// Placeholder in case we want to do anything else to the events that the observable doesn't offer

export default class ExternalEventEmitter extends Observable {
}
